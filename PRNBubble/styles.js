import {StyleSheet} from 'react-native';


const styles = StyleSheet.create({
  mainContainer: {
    marginVertical:1,
    flexDirection:'row',
  },
  bubbleStyle: {
    padding:8,
  }
});

export default styles;
